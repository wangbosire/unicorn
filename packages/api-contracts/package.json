{
  "name": "@unicorn/api-contracts",
  "private": true,
  "version": "0.1.0",
  "types": "./src/index.ts",
  "source": "./src/index.ts",
  "sideEffects": false,
  "scripts": {
    "build": "tsc --noEmit",
    "typecheck": "tsc --noEmit",
    "clean:src-artifacts": "find src -type f \\( -name '*.js' -o -name '*.js.map' -o -name '*.d.ts' \\) -delete 2>/dev/null || true",
    "clean": "pnpm run clean:src-artifacts"
  },
  "devDependencies": {
    "typescript": "~6.0.3"
  }
}
